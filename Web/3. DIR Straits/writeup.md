Python's HTTP server enables directory listings by default. Visiting `assets/img/` reveals two files: `dctflogo.png` and `flag.png`.

Opening `flag.png` reveals the flag `d:ctf{insecurity-through-obscurity}`